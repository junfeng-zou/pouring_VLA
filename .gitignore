# -----------------------------------------------------------------------------
# Python
# -----------------------------------------------------------------------------
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.manifest
*.spec
pip-log.txt
pip-delete-this-directory.txt

# Type check / test / coverage
.mypy_cache/
.pytest_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# -----------------------------------------------------------------------------
# Virtual environments
# -----------------------------------------------------------------------------
.venv/
# 仅忽略仓库根目录下的虚拟环境目录，避免误伤 scripts/env/ 等源码目录
/venv/
/env/
/ENV/
.virtualenv/

# -----------------------------------------------------------------------------
# Packaging / build
# -----------------------------------------------------------------------------
*.egg-info/
*.egg
dist/
build/
wheels/
*.whl

# -----------------------------------------------------------------------------
# Secrets & local config (do not commit)
# -----------------------------------------------------------------------------
.env
.env.*
!.env.example
*.pem
secrets.yaml
secrets.yml

# -----------------------------------------------------------------------------
# IDE / editor (optional: remove .vscode/ line if you want shared launch.json)
# -----------------------------------------------------------------------------
.vscode/
.idea/
*.swp
*.swo
*~
.project
.settings/

# -----------------------------------------------------------------------------
# OS
# -----------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# -----------------------------------------------------------------------------
# Jupyter
# -----------------------------------------------------------------------------
.ipynb_checkpoints/
*.ipynb_checkpoints

# -----------------------------------------------------------------------------
# Isaac Sim / NVIDIA Omniverse (local kit install or symlinks)
# -----------------------------------------------------------------------------
_isaac_sim/
*.lock

# -----------------------------------------------------------------------------
# Weights, checkpoints, datasets (large — keep local / use Git LFS if you must)
# -----------------------------------------------------------------------------
checkpoints/
data/
assets/
*.pdf
*.hdf5
*.h5
*.pt
*.pth
*.ckpt
*.safetensors
*.onnx
*.mlmodel

# Fine-tuning runs (LoRA adapters, intermediate checkpoints, copied tokenizers)
LoRA_train/runs/
# Repo-root training outputs (checkpoints, tokenizers copied from training)
/runs/

# Experiment tracking
wandb/
mlruns/
lightning_logs/

# Hugging Face download cache if created inside the repo
**/.cache/

# -----------------------------------------------------------------------------
# Logs (keep small markdown logs in log/ if you track them — only ignore raw logs)
# -----------------------------------------------------------------------------
*.log
logs/

# -----------------------------------------------------------------------------
# Misc temp
# -----------------------------------------------------------------------------
*.tmp
*.bak
*.orig

# -----------------------------------------------------------------------------
# RViz HDF5 replay — URDF and mesh assets (large CAD; keep local)
# -----------------------------------------------------------------------------
scripts/robot/rviz_replay/urdf/
scripts/robot/rviz_replay/meshes/

# -----------------------------------------------------------------------------
# Gripper — track source in git; ignore docs, requirements, and build artifacts
# -----------------------------------------------------------------------------
tools/gripper/*.md
tools/gripper/requirements.txt
tools/gripper/*.hex
tools/gripper/*.ihx
tools/gripper/*.lk
tools/gripper/*.lst
tools/gripper/*.map
tools/gripper/*.mem
tools/gripper/*.rel
tools/gripper/*.rst
tools/gripper/*.sym

# Local-only LoRA training server entrypoint (not shared)
LoRA_train/code/train_lora_server.py

# Debug images & local agent / scratch notes (repo root)
/debug_*.jpg
/cmd.md
